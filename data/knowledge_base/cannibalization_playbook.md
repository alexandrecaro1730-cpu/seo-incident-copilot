# Keyword Cannibalization Playbook

When a commercial landing page loses rankings while another URL on the same domain becomes visible for the same query, treat this as a cannibalization risk. Validate ranking URL history, internal anchor text, canonical tags, and content intent. Recommended actions include clarifying the primary target page, reducing conflicting optimization on the secondary page, updating internal links, and adding canonical or noindex controls only when they match the content strategy.
